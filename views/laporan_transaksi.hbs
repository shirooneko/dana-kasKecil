<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laporan Transaksi</title>
    <link rel="stylesheet" href="../assets/bootstrap-5.0.2-dist/css/bootstrap.min.css">

    <style>
        @media print {
            .print-button {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="mt-5">
            <div class="col-lg-10 mt-5">
                <div>
                    <h4>Laporan Transaksi satu bulan {{bulan}}</h4>
                </div>
                {{#if transaksi}}
                <div class="d-flex align-items-center">
                    <table class="table table-striped table-bordered mt-5">
                        <thead>
                            <tr>
                                <th scope="col">No</th>
                                <th scope="col">Keterangan</th>
                                <th scope="col">No Bukti</th>
                                <th scope="col">Debet</th>
                                <th scope="col">Kredit</th>
                                <th scope="col">Saldo</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each transaksi}}
                            <tr>
                                <td>{{ inc @index }}</td>
                                <td>{{ no_akun}} {{nm_akun}}</td>
                                <td>{{ no_bukti }}</td>
                                <td>{{ formatRupiah kas_masuk }}</td>
                                <td>{{ formatRupiah kas_keluar }}</td>
                                <td>{{ formatRupiah saldo}}</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
                <button class="print-button btn btn-primary" onclick="window.print()">Print this page</button>
                <a href="/laporan" class="print-button btn btn-danger">Kembali</a>
                {{else}}
                <p>Tidak ada transaksi untuk bulan yang dipilih.</p>
                {{/if}}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>